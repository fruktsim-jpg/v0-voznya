// Test stub for Next.js `server-only`. The real package throws if imported
// into a client bundle; in unit tests there is no bundler boundary, so it's a
// no-op. Lets us import server-only modules to test their PURE logic.
export {}
